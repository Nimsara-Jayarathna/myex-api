version: "3.9"

services:
  api:
    build: .
    container_name: blipzo-api
    env_file: .env
    environment:
      - PORT=${PORT:-5000}
      - MONGO_URI=${MONGO_URI:-mongodb://mongodb:27017/blipzo}
      - MONGO_DB_NAME=${MONGO_DB_NAME:-blipzo}
    ports:
      - "${PORT:-5000}:5000"
    depends_on:
      - mongodb
    networks:
      - blipzo-api_default
      - npm_default   # <- Add this line
    restart: unless-stopped

  mongodb:
    image: mongo:7
    container_name: blipzo-mongodb
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db
    networks:
      - blipzo-api_default   # Only internal API network

volumes:
  mongo-data:

networks:
  blipzo-api_default:
    driver: bridge
  npm_default:
    external: true   # <- tells Compose to use existing npm_default network
