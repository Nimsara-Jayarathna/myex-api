# Public API v2 placeholder

Add future `/api/v2/*` controllers here when a breaking public API change is required.
Keep business logic inside `src/modules/*` and only version controllers/DTOs/mappers.
